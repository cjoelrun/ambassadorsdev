module UsersHelper
  def local_address(id)
    html = '';
    user = User.find(params[:id])
    if (user.local_street.blank?())
      html += <<-HTML
      <p>blah#{user.local_street}</p>
      HTML
    end
    
    html.html_safe
  end
end
