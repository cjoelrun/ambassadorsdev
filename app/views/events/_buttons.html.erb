<div class="btn-group">
  <%= link_to raw('<i class="icon-arrow-left"></i> Back'), events_path, :class => "btn" %>
  <%= link_to " Edit", edit_event_path(@event), :class => "btn" %>
  <%= link_to "Delete", event_path, confirm: 'Are you sure?', method: :delete, :class => 'btn'%>
</div>
