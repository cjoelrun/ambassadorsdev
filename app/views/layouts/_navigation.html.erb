<ul class="nav">
  <% if user_signed_in? %>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">Events <b class="caret"></b></a>
      <ul class="dropdown-menu">
	<li>
	  <%= link_to('Events', events_path) %>
	</li>
	<li>
	  <%= link_to('My Events', user_event_registrations_path(current_user)) %>
	</li>
	<% if current_user.has_role? :admin %>
	  <li>
	    <%= link_to('Create Event', new_event_path) %>
	  </li>
	<% end -%>
      </ul>
    </li>

    <li>
      <%= link_to('Users', users_path) %>
    </li>
    <% if current_user.has_role? :admin %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li>
	    <%= link_to "Registrations Statuses", registration_statuses_path %>
	  </li>
	  <li>
	    <%= link_to "Credit Types", credit_types_path %>
	  </li>
	  <li>
	    <%= link_to "Event Types", event_types_path %>
	  </li>
          <li>
	    <%= link_to "Apparels", apparels_path %>
	  </li>
	  <li>
	    <%= link_to "Committees", committees_path %>
	  </li>
	  <li>
	    <%= link_to "Years", years_path %>
	  </li>
        </ul>
      </li>
    <% end %>
  <% end -%>
</ul>

<ul class="nav pull-right">
  <li class="dropdown">
    <% if user_signed_in? %>
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <b class="caret"></b></a>
    <% else %>
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <b class="caret"></b></a>
    <% end %>
    <ul class="dropdown-menu">
      <% if user_signed_in? %>
	<li>
	  <%= link_to('View account', user_path(current_user)) %>
	</li>
	<li>
	  <%= link_to('Edit account', edit_user_registration_path) %>
	</li>
	<li class="divider"></li>
      <% end -%>

      <% if user_signed_in? %>
	<li>
	  <%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>        
	</li>
      <% else %>
	<li>
	  <%= link_to('Login', new_user_session_path)  %>
	</li>
	<li>
	  <%= link_to('Sign up', new_user_registration_path)  %>
	</li>
      <% end %>
    </ul>
  </li>
</ul>
